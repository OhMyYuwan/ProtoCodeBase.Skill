# Scripts

Reserved for deterministic helpers used by the ACP v1.0.0 skill.

The initial release intentionally keeps scripts empty. The skill should first
try to operate from project manifests and bundled templates; add scripts only
when a repeated operation benefits from deterministic execution.
